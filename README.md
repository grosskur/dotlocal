# dotlocal

This is a collection of commands that is managed declaratively using
[dotslash](https://github.com/facebook/dotslash).

## Setup

Copy all binaries in the `bin` directory into `~/.local/bin`, and make
sure the latter is on your `$PATH`.
